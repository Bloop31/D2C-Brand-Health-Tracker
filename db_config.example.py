"""
Template for db_config.py - copy this file to db_config.py and fill in
your real credentials. db_config.py itself is gitignored so your
password never gets committed to GitHub.
"""

DB_CONFIG = {
    "host": "localhost",
    "port": 5432,
    "dbname": "d2c_tracker",
    "user": "postgres",
    "password": "yourpassword",
}
